

#include "handlers.h"

using namespace fos;

void TickHandler(const double elapsedSeconds, TickTime* tickTime)
{
	//Globals::messenger->write(boost::format("I: elapsedSeconds = %.9f\n") % elapsedSeconds);
}

